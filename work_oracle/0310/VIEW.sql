CREATE OR REPLACE VIEW V_EMP2
AS 
SELECT E.ENAME
     , D.DNAME
  FROM EMP E, DEPT D
  WHERE E.DEPTNO=D.DEPTNO;
  
SELECT * FROM V_EMP2;


--VIEW는 인덱스 생성 불가!
CREATE INDEX IDX_V_EMP1
ON V_EMP1(ENAME);

CREATE OR REPLACE VIEW V_EMP1
AS 
SELECT EMPNO
     , ENAME
     , JOB
     , HIREDATE
  FROM EMP;

--롤백해도 뷰는 취소되지 않음
ROLLBACK;

CREATE VIEW V_EMP1
AS 
SELECT EMPNO
     , ENAME
     , JOB
     , HIREDATE
  FROM EMP;

DROP VIEW V_EMP1;

SELECT *
  FROM V_EMP1;
  
SELECT *
  FROM EMP;